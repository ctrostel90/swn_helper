import { ShipBuilderAdapter, ShipBuilderView} from "views/ship_create_view.slint";
import { ShipListView, ShipListAdapter } from "widgets/ship_list_view.slint";
import {MenuBar, MenuBarAdapter } from "views/menu_bar_view.slint";
import {ShipHullComboAdapter} from "views/combo_tables/ship_hull_table.slint";

export { ShipBuilderAdapter , ShipListAdapter, ShipHullComboAdapter}
export { MenuBarAdapter }
export component MainWindow inherits Window{
    title: "SWN Ship Editor";
    //width: 1200px;
    VerticalLayout {
        MenuBar {}
        HorizontalLayout {
            ShipListView {
                width: 400px;
            }
            ShipBuilderView {

            }
        }
    }
}